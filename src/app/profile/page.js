import React from 'react';
import ProfilePageClient from './ProfilePageClient';

export const metadata = {
  title: "WashHub | Profile"
};

const ProfilePage = () => {
  return <ProfilePageClient />;
};

export default ProfilePage;
